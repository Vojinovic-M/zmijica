export const Position = (x, y) => ({ x, y });
export const Velocity = (dx, dy) => ({ dx, dy });
export const Snake = () => ({});
export const Food = () => ({});
export const GameOver = () => ({});
export const Score = (value) => ({ value });